# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment variables (secrets)
.env
!.env.example

# Model weights (large, downloaded by scripts)
models/

# Evaluation data (large, downloaded by scripts)
eval/data/

# Precomputed dictionaries (generated by precompute.py)
server/data/

# Node.js (frontend demo)
demo/node_modules/
demo/.next/
demo/.env.local

# Docker
*.log

# Compiled requirements (generated; commit this for reproducibility)
# requirements.txt  <-- intentionally NOT ignored, commit the lock file

# Locust results (commit these as evidence)
# loadtest/results/  <-- intentionally NOT ignored

# Eval results (commit these as evidence)
# eval/results/  <-- intentionally NOT ignored

# Cost results (commit these as evidence)
# cost/results/  <-- intentionally NOT ignored

# Temporary files
*.tmp
*.bak
*.swp

# Python test / type / lint / coverage caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Node (catch node_modules anywhere, not only under demo/)
node_modules/
*.tsbuildinfo

# Model weight formats (belt-and-suspenders; models/ is already ignored)
*.pt
*.bin
*.onnx

# Datasets / archives (belt-and-suspenders; eval/data/ is already ignored)
*.tar
*.tar.gz
*.zip

# Full per-word eval predictions embed the dataset: keep local, commit the
# slimmed *.summary.json instead (see eval/summarize.py)
eval/results/ct2_int8.json
eval/results/baseline_fp32.json

# Internal-working / assignment docs (not part of the public deliverable)
docs/assignment.md
docs/project_plan.md
claude.md